/*
Problem: Redundant Connection (LeetCode 684)
Pattern: Graphs (BFS/DFS on Grid + General)
Approach: Union-Find. Add edges one at a time — the first edge that tries to connect two nodes already in the same set is the redundant one.
Time: O(n) with union find (near O(1) per op) | Space: O(n)
*/

// code goes here once solved
