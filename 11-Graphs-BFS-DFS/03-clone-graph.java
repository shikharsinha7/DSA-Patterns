/*
Problem: Clone Graph (LeetCode 133)
Pattern: Graphs (BFS/DFS on Grid + General)
Approach: DFS or BFS while keeping a hashmap from original nodes to their cloned versions, so you never clone the same node twice and cycles don't cause infinite loops.
Time: O(V + E) | Space: O(V)
*/

// code goes here once solved
