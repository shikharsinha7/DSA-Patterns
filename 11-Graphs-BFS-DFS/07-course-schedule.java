/*
Problem: Course Schedule (LeetCode 207)
Pattern: Graphs (BFS/DFS on Grid + General)
Approach: Detect a cycle in a directed graph using DFS with visited/visiting states (or Kahn's algorithm with in-degrees). If there's a cycle, you can't finish all the courses.
Time: O(V + E) | Space: O(V + E)
*/

// code goes here once solved
